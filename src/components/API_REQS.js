const pubgApiToken = process.env.REACT_APP_API_KEY
const BASE_URL = "https://api.pubg.com/shards/steam/"

const headers = {
    headers: {
        Accept: "application/vnd.api+json",
        Authorization: API_KEY
    }
}

const season = {
    20: ,
}

const gameMode = {
    squads: 'gameMode/squad-fpp/',
    duos: 'gameMode/duos-fpp/',
    solo: 'solo-fpp'
}

exampleRequest = {

}

